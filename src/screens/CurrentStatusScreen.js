import React, { Component } from 'react';
import {
    ScrollView,
    Text,
    TextInput,
    View,
    Button
} from 'react-native';

export default class CurrentStatusScreen extends Component {
  render() {
    return (
      <Text>CurrentStatus</Text>
    );
  }
}
